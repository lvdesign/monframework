{# {{ csrf_input() }} #}
<div class="row">
    <div class="col-md-6">{{ field('name', item.name, "Titre de l'article") }}</div>
    <div class="col-md-6">{{ field('category_id', item.category_id, "Categorie", { options: categories}) }}</div>
</div>
<div class="row">  

    <div class="col-md-4">
    {% if item.image %}
    <img src="{{ item.thumb }}" alt="{{ item.name }}" style="width:50%" />
     {% endif  %}
    </div>

    <div class="col-md-4">{{ field('image', item.image, "Image", { type: 'file'} ) }}</div>

    <div class="col-md-4">{{ field('slug', item.slug, "Lien") }}</div>
</div>

{{ field('content', item.content, "content", {type: 'textarea'}) }}
{{ field('created_at', item.created_at, "Date de création", { class: 'datepicker'}) }}